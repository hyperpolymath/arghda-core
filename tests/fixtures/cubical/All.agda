{-# OPTIONS --cubical --safe #-}

-- A Cubical-Agda file: --cubical --safe, deliberately NOT --without-K
-- (cubical is incompatible with --without-K). The standard Agda lint pack
-- would wrongly flag this as missing --without-K; the cubical pack accepts it.
module All where
